{
  "name": "semver",
  "version": "5.4.1",
  "description": "The semantic version parser used by npm.",
  "main": "semver.js",
  "scripts": {
    "test": "tap test/*.js --cov -J"
  },
  "devDependencies": {
    "tap": "http://github.com/jBarz/node-tap/archive/v10.7.0.zos.tar.gz"
  },
  "license": "ISC",
  "repository": "https://github.com/npm/node-semver",
  "bin": {
    "semver": "./bin/semver"
  },
  "files": [
    "bin",
    "range.bnf",
    "semver.js"
  ]
}
